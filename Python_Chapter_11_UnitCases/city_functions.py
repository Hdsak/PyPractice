def city(country, city):
    full=country.title()+", "+city.title()
    print(full)
    return full
def city_task_2(country,city,population):
    full=country.title()+", "+city.title()+" "+str(population)
    print(full)
    return full